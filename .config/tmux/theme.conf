# Generated by the `theme` skill (theme.py). Do not edit by hand.

if-shell "defaults read -g AppleInterfaceStyle 2>/dev/null | grep -q Dark" \
    "set -g status-style bg=default,fg='#ebdbb2'; \
     set -g window-status-style bg=default,fg='#ebdbb2'; \
     set -g window-status-current-style bg=default,fg='#cc241d'" \
    "set -g status-style bg=default,fg='#575279'; \
     set -g window-status-style bg=default,fg='#575279'; \
     set -g window-status-current-style bg=default,fg='#b4637a'"

if-shell "defaults read -g AppleInterfaceStyle 2>/dev/null | grep -q Dark" \
    "set -g status-left '#[fg=#ebdbb2,bg=default,bold] #{?client_prefix,󰠠 ,#[dim]󰤂 }#[bold,nodim]#S$hostname '; \
     set -g status-right '#[bg=default,fg=#d79921,noitalics,bold] %Y-%m-%d %H:%M '; \
     set -g window-status-current-format '#[bg=default,fg=#d25e1f] #W  '; \
     set -g window-status-format '#[bg=default,fg=#ebdbb2] #W '" \
    "set -g status-left '#[fg=#575279,bg=default,bold] #{?client_prefix,󰠠 ,#[dim]󰤂 }#[bold,nodim]#S$hostname '; \
     set -g status-right '#[bg=default,fg=#575279,noitalics,bold] %Y-%m-%d %H:%M '; \
     set -g window-status-current-format '#[bg=default,fg=#b4637a] #W   '; \
     set -g window-status-format '#[bg=default,fg=#575279] #W '"
