# Remote tmux: retain sessions and let the outer tmux forward mouse scrolling.
set -g mouse on
set -g history-limit 100000
setw -g mode-keys vi
# Keep a different prefix from the laptop's Ctrl-Space.
set -g prefix C-b
bind C-b send-prefix
